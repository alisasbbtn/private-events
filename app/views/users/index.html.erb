<%= provide(:title, 'Users') %>

<div class="row">
  <div class="col-md-4">
    <h1>Users</h1>
  </div>

  <div class="col-md-8 list-group">
      <%= render @users %>
      <div class="text-center"><%= will_paginate @users %></div>
  </div>
</div>
